version: "3.9"
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: app
    stdin_open: true
    tty: true
    ports:
      - 5173:5173
    volumes:
      - ".:/app"
      - "/app/node_modules"
